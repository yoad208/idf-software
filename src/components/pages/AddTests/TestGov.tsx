import { Box, Button } from '@mui/material';

export const TestGov = () => {
  return (
    <Box>
      <Button color="primary" variant="contained">
        להריץ בדיקה
      </Button>
    </Box>
  );
};
